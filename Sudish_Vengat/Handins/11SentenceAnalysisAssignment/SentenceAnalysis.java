public class SentenceAnalysis{

private	int wordlength;
private	int numberofwords;

public int getWordLength(){
	return wordlength;
}

public void setWordLength(int newLength){
	wordlength = newLength;
}

public int getNumberOfWords(){
	return numberofwords;
}

public void setNumberOfWords(int newNumber){
	numberofwords = newNumber;
}

//int or String m?
public int sumOfFirstWordLength(){	 	
	 if while(m.find()){
		count++;
		system.out.println("Count word length" + m.count());
		system.out.println("Start counting word length process" + m.start());
		system.out.println("End counting word length process" + m.end());
	} else {
		return "Error I can't count";
	}
}

//int or String s?
public int sumOfSecondWordLength(){
	if while(s.find()){
		count++;
		system.out.println("Count word length" + s.count());
		system.out.println("Start counting word length process" + s.start());
		system.out.println("End counting word length process" + s.end());
	} else {
		return "Error I can't count";
	}
}

public String toString(){
	return "HI";
}
}